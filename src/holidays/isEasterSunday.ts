/**
 * Returns if a given date is Easter Sunday.
 *
 * @todo Determine how Easter Sunday is defined per NZ legislation
 */
export function isEasterSunday(date: Date): boolean {
	// TODO

	return false;
}
